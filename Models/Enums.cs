namespace gestor_tareas_api.Models
{
    public enum RolProyecto { Owner, Editor, Viewer }
    public enum EstadoTarea { ToDo, InProgress, Done }
    public enum PrioridadTarea { Baja, Media, Alta }
}
